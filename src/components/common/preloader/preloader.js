import React from 'react';
import preloader from './preloader.svg'

const Preloader = () => {
    return <img style={{width: 200}}   src={preloader} alt={preloader} />
    
}

export default Preloader;